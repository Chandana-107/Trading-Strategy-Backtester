[tool.poetry]
name = "trading-backtester"
version = "0.1.0"
description = "A trading strategy backtester implemented in Python."
authors = ["Your Name <youremail@example.com>"]
license = "MIT"
packages = [{include = "backtester", from = "src"}]

[tool.poetry.dependencies]
python = "^3.8"
yfinance = "^0.2.0"
pandas = "^2.0.0"
matplotlib = "^3.7.0"
numpy = "^1.24.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
